<div class="mt-3">
  <div class="row justify-content-center">
    <section class="col-lg-5">
        <div class="col-xs-12">
            <h3>Connectez-vous</h3>
            <%= form_for :user, url: new_session_path do |form| %>
                <div class="input-group mb-2">
                <%= form.text_field :name, placeholder: "Nom d'utilisateur" , :class => "form-control"%> 
                </div>
                <div class="input-group mb-2">
                <%= form.password_field :password, placeholder: "Mot de passe" , :class => "form-control"%> 
                </div>
                <div class="form-group">
                  <button type="submit" class="btn btn-primary">Se connecter</button>
                  <a href="<%= new_password_path %>" class="btn btn-link">Mot de passe oublié</a>
                </div>
            <% end %>
            <div class="form-group">
                <div class="topCushion">Pas encore membre ? <a href="<%= new_user_path %>">S'inscrire</a></div>
            </diV>
        </div>       
    </section>
  </div>
</div>